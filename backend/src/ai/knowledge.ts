/** Static operational knowledge for the advisor agent (no filesystem dependency at runtime). */
export const PROJECT_WORKFLOW = `# 巡牌通 · PatrolPlate 完整工作流程

## 系统架构概览

平台有两条可组合的链路：

1. **兼容 v1 本机控车**：浏览器 → WebSocket 本机网关 (127.0.0.1:8787) → 小车 TCP :6000；视频 HTTP :6500。
2. **平台模式**：浏览器 → Fastify API → PostgreSQL；边缘 agent 上报 GPS 遥测；巡检任务由调度器经 Nav2 执行。

## 日常运营主流程

1. **登录**：操作员或管理员使用账号密码登录平台（会话 Cookie）。
2. **维护白名单**：在「白名单管理」添加/导入已登记车牌（私家车或访客）。白名单为空时无法启动巡检（HTTP 409）。
3. **选择设备**：在「设备管理」选择当前巡检车（或工作台顶栏当前小车）。
4. **启动巡检**：在「巡检任务」选择路线与班次并启动。系统将当前全局白名单复制为任务不可变快照。
5. **边缘采集**：小车按航点巡检，上报车牌观测；置信度 < 0.75 进入待复核；禁停区命中记为违规停车。
6. **分类**：
   - registered_private：白名单私家车
   - visitor：白名单访客
   - suspected_external：疑似外来/闯入
   - pending_review：低置信度待审核
7. **人工审核**：在「待人工审核」处理低置信度识别。
8. **违规与上门处置**：禁停区登记车可生成上门处置候选；物业人工确认后派车留证（不得由 AI 直接控车）。
9. **任务级报告**：任务结束后可打开 FR-007 HTML/CSV 巡检报告。
10. **AI 日报**：在「报告中心」手动生成指定日叙事分析报告（闯入车、乱停等）。

## 小车无法连接时常见原因

- 2 分钟内无遥测且 last_seen 过期 → 设备离线（检查边缘 agent / 网络 / Bridge）
- 存在其他用户的有效控制租约 → 控制权被占用
- 该车有 running/queued 巡检任务 → 与人工控车互斥
- bridge_url 为空 → 平台边缘链路未配置
- TCP 主机/端口配置错误 → 需管理员在设备管理核对（网关不向前端暴露原始 TCP 报文）

## 重要安全规则

- 浏览器不得发送原始 TCP 控车命令；控车必须经本机网关 + 有效租约。
- AI/顾问仅提供查询与建议，不得建议 AI 直接控制车辆或发送 TCP。
- Front 按钮报文仍有证据冲突，真车验证以前不得声称兼容性已证实。
`;
